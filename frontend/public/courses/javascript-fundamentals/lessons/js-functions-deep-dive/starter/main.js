// applyTwice(f, x) returns f(f(x)).
//
// Examples:
//   applyTwice(n => n + 3, 10)        // 16
//   applyTwice(s => s + "!", "hi")    // "hi!!"
//   applyTwice(n => n * 2, 3)         // 12
//
// The checker calls applyTwice(...) for you with different f's and x's.
// Make sure to RETURN the result — not console.log it.

function applyTwice(f, x) {
  // TODO: call f on x, then call f on that result, and return the final value.
}
