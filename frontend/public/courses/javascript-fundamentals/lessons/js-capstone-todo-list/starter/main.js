// Capstone: Todo List
//
// Four functions to write, then a one-line driver at the bottom that
// proves your code works end-to-end. Every update is IMMUTABLE — you
// return a new array, you don't change the input.

// 1) addTodo — append a new todo with a fresh id (max existing + 1, or 1)
function addTodo(todos, text) {
  // TODO
}

// 2) toggleTodo — return a new array with the matching todo's `done` flipped
function toggleTodo(todos, id) {
  // TODO
}

// 3) activeTodos — return only the todos with done === false
function activeTodos(todos) {
  // TODO
}

// 4) stats — return { total, active, completed }
function stats(todos) {
  // TODO
}

// 5) Driver — write this last, after the four functions above pass.
//    Add three todos ("Buy groceries", "Finish report", "Call dentist").
//    Toggle the second one (id=2) as done.
//    Print: Total: 3, Active: 2, Completed: 1
//
// Hint: you'll be reassigning `todos` each time because every function
// returns a NEW array.
//
// (The expected output is one line.)
