# Functions — Temperature Converter

# TODO: Define celsius_to_fahrenheit(c) that returns the Fahrenheit value
# Formula: F = C * 9/5 + 32

# TODO: Define classify_temp(f) that returns "freezing", "cold",
# "comfortable", or "hot" based on the Fahrenheit value

# The block below only runs when you click Run — the grader tests your
# functions directly, so it skips this part.
if __name__ == "__main__":
    celsius = float(input("Enter temperature in Celsius: "))

    # TODO: Convert, classify, and print the results
    # Format: "22°C = 71.6°F"
    # Format: "Classification: comfortable"
