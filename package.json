{
  "name": "codetutor-ai",
  "version": "0.1.0",
  "private": true,
  "description": "Root-level coordination package. Holds cross-cutting dev tools (currently just the Supabase CLI) so contributors get them via a single npm install. The three real packages live under backend/, frontend/, and e2e/.",
  "scripts": {
    "supabase": "supabase"
  },
  "devDependencies": {
    "supabase": "^2.90.0"
  }
}
