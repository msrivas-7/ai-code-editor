#cloud-config
# First-boot VM bootstrap. Installs Docker + Azure CLI, clones the repo,
# pulls secrets from Key Vault via the VM's managed identity, builds the
# compose images (locally until Phase 19c publishes to GHCR), and hands
# long-running lifecycle to a systemd unit.
#
# Templated at deploy time by main.bicep:
#   {{KV_NAME}}       — Key Vault name to read secrets from
#   {{VM_FQDN}}       — Caddy cert hostname
#   {{SWA_HOSTNAME}}  — set as backend CORS_ORIGIN
#   {{REPO_URL}}      — public repo to clone (no auth needed)
#   {{BACKEND_IMAGE}} — GHCR tag the backend service pulls (post-19c)
#   {{RUNNER_IMAGE}}  — GHCR tag for the runner image
#   {{ADMIN_EMAIL}}   — LE registration address + Caddy contact

package_update: true
package_upgrade: false

packages:
  - ca-certificates
  - curl
  - gnupg
  - git
  - jq
  - unattended-upgrades

write_files:
  # Per-VM identity + bootstrap config consumed by /usr/local/bin/refresh-env
  # on every fire. KV_NAME drives Key Vault lookups (managed-identity-bound);
  # the four image/host vars get echoed straight into .env. First-boot
  # writes this file from cloud-init template substitution; subsequent
  # edits are manual (rare — only if the VM is renamed or images move).
  #
  # Why split refresh-env's identity vars out of the script body: prior shape
  # baked {{KV_NAME}} + siblings into a write_files inline at first-boot,
  # which then never re-synced when cloud-init.yaml gained new fetch_optional
  # lines. Keeping identity here + script in infra/scripts/refresh-env.sh
  # (deploy-synced) closes that drift gap. See infra/scripts/refresh-env.sh.
  - path: /etc/codetutor/env.conf
    permissions: '0644'
    owner: root:root
    content: |
      KV_NAME={{KV_NAME}}
      BACKEND_IMAGE={{BACKEND_IMAGE}}
      RUNNER_IMAGE={{RUNNER_IMAGE}}
      VM_FQDN={{VM_FQDN}}
      ADMIN_EMAIL={{ADMIN_EMAIL}}

  - path: /etc/systemd/system/codetutor-ai.service
    permissions: '0644'
    owner: root:root
    content: |
      [Unit]
      Description=CodeTutor AI backend stack
      Requires=docker.service
      After=docker.service network-online.target
      Wants=network-online.target

      [Service]
      Type=simple
      User=codetutor
      Group=codetutor
      WorkingDirectory=/opt/codetutor
      # Re-pull secrets on every start — covers rotation + first boot.
      ExecStartPre=/usr/local/bin/refresh-env
      ExecStart=/usr/bin/docker compose -f docker-compose.yml -f docker-compose.prod.yml --env-file .env up
      ExecStop=/usr/bin/docker compose -f docker-compose.yml -f docker-compose.prod.yml --env-file .env down
      Restart=on-failure
      RestartSec=30
      # First boot downloads images / builds locally, so allow plenty of time.
      TimeoutStartSec=900

      [Install]
      WantedBy=multi-user.target

  - path: /etc/systemd/system/codetutor-ai-refresh-env.service
    permissions: '0644'
    owner: root:root
    content: |
      [Unit]
      Description=Refresh CodeTutor .env from Key Vault (idempotent)

      [Service]
      Type=oneshot
      User=codetutor
      Group=codetutor
      WorkingDirectory=/opt/codetutor
      ExecStart=/usr/local/bin/refresh-env
      # `up -d` is a no-op if env is unchanged; restarts the affected
      # services if any value rotated.
      ExecStartPost=/usr/bin/docker compose -f docker-compose.yml -f docker-compose.prod.yml --env-file .env up -d

  - path: /etc/systemd/system/codetutor-ai-refresh-env.timer
    permissions: '0644'
    owner: root:root
    content: |
      [Unit]
      Description=Daily refresh of CodeTutor .env from Key Vault

      [Timer]
      OnCalendar=daily
      RandomizedDelaySec=1h
      Persistent=true

      [Install]
      WantedBy=timers.target

  # Hourly sweep of stale session workspaces. Startup purge in the backend
  # handles orphans across restarts; the in-process idle sweeper
  # (config.session.idleTimeoutMs) catches stale sessions within a few
  # minutes of going idle. This timer exists as a safety net for the case
  # where the backend crashed / was stopped before its own sweeper could
  # run. Running sessions keep touching their dir via the runner mount so
  # mtime stays fresh and they're never caught by this sweep.
  #
  # M-4 (bucket 7): widened 120 min → 24 h. Two reasons:
  #   (1) the audit's open question was "does backend idle-sweep catch
  #       sessions before 120 min"? At config.session.idleTimeoutMs ≈ 45 m
  #       the answer is yes, so this timer was racing legitimately-paused
  #       sessions. 24 h removes that race.
  #   (2) any backend downtime shorter than 24 h will NOT leave a session
  #       dir exposed to this sweep by the time backend restarts and can
  #       reconcile against the live-sessions list.
  - path: /etc/systemd/system/codetutor-session-cleanup.service
    permissions: '0644'
    owner: root:root
    content: |
      [Unit]
      Description=Prune stale CodeTutor session workspaces
      ConditionPathIsDirectory=/opt/codetutor/temp/sessions

      [Service]
      Type=oneshot
      ExecStart=/usr/bin/find /opt/codetutor/temp/sessions -mindepth 1 -maxdepth 1 -type d -mmin +1440 -exec rm -rf {} +

  - path: /etc/systemd/system/codetutor-session-cleanup.timer
    permissions: '0644'
    owner: root:root
    content: |
      [Unit]
      Description=Hourly prune of stale CodeTutor session workspaces

      [Timer]
      OnBootSec=15min
      OnUnitActiveSec=1h
      Persistent=true

      [Install]
      WantedBy=timers.target

  # S-4 (bucket 7): daily TLS-cert expiry probe. Caddy auto-renews LE certs
  # ~30 days before expiry, so a silent renewal failure is only visible once
  # the cert has already broken in a browser. This probe hits the public 443
  # endpoint, parses notAfter, and logs a warning below a 14-day threshold —
  # well inside the 30-day renewal window, so a single failed probe is a
  # real problem, not normal behavior.
  #
  # Logs land on the journal via `logger`; the Azure Monitor Agent scrapes
  # journal into the Syslog table when enabled. Until Syslog ingestion is
  # wired, tail with `journalctl -t codetutor-cert-check` on the VM.
  - path: /usr/local/bin/cert-expiry-check
    permissions: '0755'
    owner: root:root
    content: |
      #!/bin/bash
      set -euo pipefail

      FQDN="{{VM_FQDN}}"
      WARN_DAYS=14

      # `openssl s_client` can wedge waiting for stdin without the echo pipe.
      EXPIRY=$(echo \
        | openssl s_client -servername "$FQDN" -connect "$FQDN:443" 2>/dev/null \
        | openssl x509 -noout -enddate 2>/dev/null \
        | cut -d= -f2)

      if [ -z "$EXPIRY" ]; then
        logger -t codetutor-cert-check -p user.err \
          "CERT_EXPIRY_PROBE_FAILED fqdn=$FQDN reason=handshake-or-parse"
        exit 1
      fi

      EXPIRY_EPOCH=$(date -d "$EXPIRY" +%s)
      NOW_EPOCH=$(date +%s)
      DAYS=$(( (EXPIRY_EPOCH - NOW_EPOCH) / 86400 ))

      if [ "$DAYS" -lt "$WARN_DAYS" ]; then
        logger -t codetutor-cert-check -p user.warning \
          "CERT_EXPIRY_WARNING days=$DAYS fqdn=$FQDN expiry=\"$EXPIRY\""
      else
        logger -t codetutor-cert-check -p user.info \
          "CERT_OK days=$DAYS fqdn=$FQDN"
      fi

  - path: /etc/systemd/system/codetutor-cert-expiry-check.service
    permissions: '0644'
    owner: root:root
    content: |
      [Unit]
      Description=Daily TLS-cert expiry probe for CodeTutor FQDN

      [Service]
      Type=oneshot
      ExecStart=/usr/local/bin/cert-expiry-check

  - path: /etc/systemd/system/codetutor-cert-expiry-check.timer
    permissions: '0644'
    owner: root:root
    content: |
      [Unit]
      Description=Daily TLS-cert expiry probe

      [Timer]
      OnCalendar=daily
      RandomizedDelaySec=1h
      Persistent=true

      [Install]
      WantedBy=timers.target

  # Auto-reboot after unattended-upgrades lands a kernel/security update.
  # Ubuntu's default installs but doesn't reboot, so CVEs sit unarmed until
  # a manual bounce. 03:00 UTC is the lowest-traffic window for a learner
  # product. systemd restart policy on codetutor-ai.service brings the stack
  # back up; Caddy on restart reconstitutes the LE cert from /etc/caddy.
  - path: /etc/apt/apt.conf.d/52unattended-upgrades-reboot
    permissions: '0644'
    owner: root:root
    content: |
      Unattended-Upgrade::Automatic-Reboot "true";
      Unattended-Upgrade::Automatic-Reboot-WithUsers "true";
      Unattended-Upgrade::Automatic-Reboot-Time "03:00";

runcmd:
  # --- 2 GB swap file ---
  # B2s ships with no swap, so a single memory spike (e.g. an OOM'ing runner
  # image build or a coincident backend + frontend dev image pull) goes
  # straight to the OOM killer. 2 GB is enough headroom to survive bursts
  # without turning the VM into a swap-thrashing toy; swappiness=10 keeps
  # the kernel preferring to reclaim page cache over swapping anon pages.
  - |
    if [ ! -f /swapfile ]; then
      fallocate -l 2G /swapfile
      chmod 600 /swapfile
      mkswap /swapfile
      swapon /swapfile
      echo '/swapfile none swap sw 0 0' >> /etc/fstab
    fi
  - echo 'vm.swappiness=10' > /etc/sysctl.d/99-codetutor.conf
  - sysctl --system

  # Wait for any in-progress apt/dpkg operation to release its lock. Ubuntu's
  # apt-daily.service systemd timer fires on boot and can race runcmd here,
  # producing "Could not get lock /var/lib/dpkg/lock-frontend" and a silently
  # failed azure-cli install. Poll the two lock paths until both are free.
  - |
    for i in $(seq 1 120); do
      if ! fuser /var/lib/dpkg/lock-frontend /var/lib/apt/lists/lock >/dev/null 2>&1; then
        break
      fi
      echo "waiting for apt lock (attempt $i/120)..."
      sleep 5
    done

  # --- Docker CE from Docker's apt repo ---
  - install -m 0755 -d /etc/apt/keyrings
  - curl -fsSL https://download.docker.com/linux/ubuntu/gpg | gpg --dearmor -o /etc/apt/keyrings/docker.gpg
  - chmod a+r /etc/apt/keyrings/docker.gpg
  - |
    echo "deb [arch=$(dpkg --print-architecture) signed-by=/etc/apt/keyrings/docker.gpg] https://download.docker.com/linux/ubuntu noble stable" > /etc/apt/sources.list.d/docker.list
  - apt-get update
  - DEBIAN_FRONTEND=noninteractive apt-get install -y docker-ce docker-ce-cli containerd.io docker-buildx-plugin docker-compose-plugin

  # --- Azure CLI (Microsoft apt repo) ---
  # Re-wait for lock in case apt-daily fired again between the docker install
  # above and now. The install script runs apt internally and won't retry.
  - |
    for i in $(seq 1 120); do
      if ! fuser /var/lib/dpkg/lock-frontend /var/lib/apt/lists/lock >/dev/null 2>&1; then
        break
      fi
      echo "waiting for apt lock before az-cli install (attempt $i/120)..."
      sleep 5
    done
  - curl -sL https://aka.ms/InstallAzureCLIDeb | bash

  # --- App user setup ---
  - usermod -aG docker codetutor
  - install -o codetutor -g codetutor -m 0755 -d /opt/codetutor
  - sudo -u codetutor git clone {{REPO_URL}} /opt/codetutor
  - install -o codetutor -g codetutor -m 0755 -d /opt/codetutor/temp/sessions

  # --- Install refresh-env from repo (deploy-synced source of truth) ---
  # Replaces the prior write_files inline. Future deploys copy this same
  # file via vm-deploy-backend.sh so adding a fetch_optional line lands
  # without a VM reprovision. env.conf above feeds it KV_NAME + identity.
  - install -m 0755 -o root -g root /opt/codetutor/infra/scripts/refresh-env.sh /usr/local/bin/refresh-env

  # --- Populate .env from KV before anything starts ---
  - /usr/local/bin/refresh-env

  # --- Pre-build images so first `compose up` doesn't block on network ---
  # Once 19c publishes to GHCR this becomes a pull-through cache warm-up.
  - cd /opt/codetutor && sudo -u codetutor docker compose -f docker-compose.yml -f docker-compose.prod.yml --env-file .env build backend runner-image

  # --- Enable systemd lifecycle ---
  - systemctl daemon-reload
  - systemctl enable --now codetutor-ai.service
  - systemctl enable --now codetutor-ai-refresh-env.timer
  - systemctl enable --now codetutor-session-cleanup.timer
  - systemctl enable --now codetutor-cert-expiry-check.timer

  # --- Security: auto-apply Ubuntu security patches (reboots at 03:00 if needed) ---
  - dpkg-reconfigure -f noninteractive unattended-upgrades

final_message: "codetutor-ai-vm cloud-init complete in $UPTIME seconds"
